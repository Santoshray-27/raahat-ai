# RAAHAT Backend

## Database Configuration
For local development and the demo, `DATABASE_URL` can be left empty in `.env`.
Before production deployment, you must provide a valid Supabase/Neon PostgreSQL URL for `DATABASE_URL`.
(Database schema will be provided by Satwik).
